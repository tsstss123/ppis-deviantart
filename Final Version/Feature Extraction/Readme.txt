bigdataset contains the images of 1 user (Swezzels) to test the code on (total dataset is too large). 
features is the folder that the calculated features will be stored in. This folder needs to exist.
src contains the matlab source code for feature extraction. 

extractFeatures.m is the main feature extraction loop. The path to the external packages is hardcoded into tis file.